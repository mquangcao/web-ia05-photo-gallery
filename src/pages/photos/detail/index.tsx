import { PhotoDetail } from "./photo-detail";

/**
 * Photo Detail Page - Shows full photo with details
 * NO logic here, only composition
 */
export default function PhotoDetailPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <PhotoDetail />
    </div>
  );
}
